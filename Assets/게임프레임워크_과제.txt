정적 멤버 변수의 정의 및 초기화를 하지  않으면 에러가 나는 이유
--> 일반 멤버 함수는 Stack영역에 메모리가 생성되자만, 초기화 되지 않은 static 변수는 Bss 영역에 할당(전역 변수도 동일)되고, 
     초기화된 static 변수는 Data영역에 할당됩니다,

     Bss영역에서는 전역/정적 변수를 자동으로 0으로 초기화 시키므로, 미리 초기화해야 합니다

     클래스 내에서 정의시, 객체 재정의시 덮어질 수 있므로, 무조건 클래스 밖에서 정의해야 합니다 

